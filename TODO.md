# Dice Card Game TODO

## Client 顯示 bug 分工（2026-05-14）

> 截圖來源：`C:\DiceCardGame\Bug\image0.png` ~ `image5.png`

### 1. 選牌階段卡片清單改為可捲動容器
負責：Client Layout Agent

- [x] 參考 `Bug/image0.png`，將紅框內的選牌卡片區改成 scroll view。
- [x] 手機版高度需限制在可視範圍內，之後卡片數量增加時只能在卡片清單內捲動，不要把底部或整頁撐爆。
- [x] 目前 10 張卡片時不得超出畫面底部，卡片 grid、間距、已選狀態需維持一致。
- [x] 確認 iPhone 尺寸下可正常滑動，且頁面主標題、進度、提示列不被捲走或遮住。

### 2. 選牌階段已選勾勾樣式一致化
負責：Card State Visual Agent

- [x] 參考 `Bug/image1.png`，檢查已選卡片右上角勾勾出現兩種外觀的原因。
- [x] 統一勾勾 icon、背景形狀、尺寸、位置與動畫結束狀態。
- [x] 若是動畫卡住，修正 transition / class state，避免半圓或殘影停留。
- [x] 確認選取、取消選取、重新進入選牌階段後狀態一致。

### 3. 送出排序等待中畫面修正
負責：Arrange Waiting UI Agent

- [x] 參考 `Bug/image2.png`，修正送出排序後等待其他玩家時的畫面排版異常。
- [x] 已送出狀態下，排序列表與底部卡片列不要互相重疊或被壓縮。
- [x] 「已送出排列」與「重置順序」等操作狀態需符合等待中語意，不能出現可誤操作的 UI。
- [x] 手機版滾動區、固定底部區與等待提示需清楚分層。

### 4. 骰完骰子後提示點擊動畫銜接修正
負責：Turn Hint Animation Agent

- [x] 參考 `Bug/image3.png`，修正骰完骰子後提示玩家點擊指定牌位的動畫不順問題。
- [x] 檢查提示動畫 keyframes / transition 的首尾狀態，讓循環前後能自然接上。
- [x] 動畫只應套用在目前可點擊的目標牌位，不要影響其他手牌。
- [x] 確認擲骰完成、輪到自己、等待對手三種狀態切換時動畫會正確開始與停止。

### 5. 卡牌詳情多餘圖案移除
負責：Card Detail Modal Agent

- [x] 參考 `Bug/image4.png`，移除卡牌詳情卡面右側紅圈處多出的奇怪圖案。
- [x] 保留正確的卡片效果 icon、分數、狀態與裝飾邊框。
- [x] 確認其他卡片類型的詳情 modal 不會因此少掉必要 icon。

### 6. 已使用卡片顯示內容簡化
負責：Used Card Display Agent

- [x] 參考 `Bug/image5.png`，使用後的卡片只保留位置資訊與「已使用」狀態。
- [x] 不要在已使用的小卡上塞入完整卡牌資訊，避免左下角畫面擁擠。
- [x] 右下「已使用」標示保留，並移除小卡上的效果 icon 與效果文字。
- [x] 確認卡片詳情 modal 仍能查看完整資訊，小卡只做精簡顯示。

本 TODO 依目前收到的 10 張前端參考圖重新整理。目標是把現有可玩的 MVP，調整成接近參考圖的直式手機遊戲 UI。

## 狀態標記

- [x] 已完成
- [ ] 待完成
- [~] 進行中

## 已完成基礎

- [x] Lobby / Room 基礎流程
- [x] 建立房間、加入房間、離開房間
- [x] 房主開始遊戲
- [x] 選牌流程：10 選 6
- [x] 排牌流程：6 張排序
- [x] 回合流程：擲骰、指定位置、翻牌、使用卡牌
- [x] 支援 2-4 人遊戲狀態
- [x] 基礎 API / smoke test

## 參考圖對照

參考圖資料夾：`docs/visual-reference/`

1. 開始畫面：主標題「骰卡傳遞」與「開始」按鈕。
2. 房間操作：暱稱、房號、加入房間、建立房間。
3. 選牌階段：10 張候選牌，已選 2/6，等待其他玩家。
4. 排牌階段：6 張牌列表排序，上下移動，送出排列。
5. 2 人回合畫面：對手在上，自己在下，中央骰子與目標位置。
6. 3 人回合畫面：兩位對手左右上方，自己在下。
7. 4 人回合畫面：上、左、右三位對手，自己在下。
8. 開房後準備畫面：房號、玩家列表、準備狀態、開始遊戲、離開房間。
9. 卡牌詳情彈窗：遊戲畫面作背景，暗化/模糊，顯示卡牌資料與效果。
10. 遊戲設定彈窗：遊戲畫面作背景，保留必要操作，移除不要的按鈕。

## P0 - 視覺基底與共用樣式

負責：UI Foundation Agent

- [ ] 建立整體視覺語言：羊皮紙底、深綠主色、金色細線、復古牌桌質感。
- [ ] 統一標題、按鈕、輸入框、卡片、狀態 pill、modal 的字級與間距。
- [ ] 統一圓角與陰影：主要卡片/面板不超過 8px 圓角，按參考圖保留紙張邊框感。
- [ ] 建立共用裝飾背景：撲克牌、骰子、水印紋理、角落裝飾。
- [ ] 確保手機直式為第一優先，桌機可置中顯示手機比例畫面。
- [ ] 移除像 landing page 的說明區塊，第一屏直接是可操作遊戲畫面。

驗收：
- [ ] 所有主要頁面看起來屬於同一套美術風格。
- [ ] 主要文字、按鈕、卡牌在手機直式不擠壓、不重疊。

## P0 - 開始畫面

負責：Start Screen Agent

- [ ] 依參考圖 1 製作全螢幕開始畫面。
- [ ] 中央紙張框內放小卡牌 icon、主標題「骰卡傳遞」。
- [ ] 下方放大型「開始」按鈕。
- [ ] 角落裝飾物不可遮住標題與按鈕。

驗收：
- [ ] 首頁不顯示房間表單或遊戲資訊，只顯示開始入口。
- [ ] 點擊「開始」後進入房間操作畫面。

## P0 - 房間操作與準備畫面

負責：Room Flow Agent

- [x] 依參考圖 2 製作房間操作畫面。
- [x] 顯示 logo / Dice Card Game 標識與「房間操作」標題。
- [x] 暱稱輸入框使用人物 icon。
- [x] 房號輸入框使用 `#` icon，旁邊放「加入」按鈕。
- [x] 下方放主要按鈕「建立房間」。
- [x] 依參考圖 8 製作開房後準備畫面。
- [x] 準備畫面顯示房號、複製房號、玩家列表、玩家狀態。
- [x] 玩家狀態只顯示「已準備」或「等待中」。
- [x] 房主可見「開始遊戲」，所有玩家可見「離開房間」。

驗收：
- [x] 房間操作頁與準備頁是同一套紙張主視覺。
- [x] 準備頁保留「複製房號」，因為這裡是開房後邀人用。

## P0 - 選牌階段

負責：Draft UI Agent

- [x] 依參考圖 3 製作選牌階段。
- [x] 頂部顯示房號、玩家數、更多設定按鈕。
- [x] 標題顯示「Game / 選牌階段」。
- [x] 顯示已選數量，例如「已選 2/6」。
- [x] 顯示等待列：「等待其他玩家選牌」與進度點。
- [x] 10 張候選牌用 2 欄 grid。
- [x] 已選牌翻開，右上顯示綠色勾勾。
- [x] 未選牌保持深綠牌背，顯示候選序號。
- [x] 底部固定「確認選牌」按鈕與說明「從 10 張候選牌中選出 6 張」。

驗收：
- [x] 未滿 6 張時確認按鈕不可用或明確弱化。
- [x] 已選狀態、候選狀態、等待狀態一眼可分辨。

## P0 - 排牌階段

負責：Arrange UI Agent

- [x] 依參考圖 4 製作排牌階段。
- [x] 標題顯示「Game / 排牌階段」。
- [x] 顯示排序進度，例如「6/6 已排」。
- [x] 提示文字：「調整 1 到 6 的牌序，送出後會傳給右側玩家」。
- [x] 6 張牌改為列表式排序。
- [x] 每列包含順位、圖示、名稱、分數、上移/下移按鈕。
- [x] 當前選中或移動中的牌使用綠色高亮框。
- [x] 底部顯示 6 個排序 slot。
- [x] 主要按鈕「送出排列」，次要按鈕「重置順序」。

驗收：
- [x] 上移/下移不造成列表跳動或文字溢出。
- [x] 送出後進入等待其他玩家或回合階段。

## P0 - 多玩家遊戲桌面

負責：Table Layout Agent

- [x] 依參考圖 5 支援 2 人桌面：對手在上，自己在下。
- [x] 依參考圖 6 支援 3 人桌面：兩位對手左右上方，自己在下。
- [x] 依參考圖 7 支援 4 人桌面：上、左、右三位對手，自己在下。
- [x] 每位玩家顯示圓形 avatar、名稱、分數、6 張牌背。
- [x] 自己手牌固定在底部，顯示 1 到 6 位置編號。
- [x] 中央顯示骰子、目標位置、目前操作按鈕。
- [x] 頂部狀態列顯示分數總覽，依玩家數自動排列。

驗收：
- [x] 2/3/4 人畫面都不能有玩家區、手牌、中央骰子互相重疊。
- [x] 4 人版列為最重要的壓力測試。

## P0 - 回合操作狀態

負責：Turn Interaction Agent

- [x] 回合標題顯示「Game / 回合階段」。
- [x] 狀態 pill 支援「輪到你」「輪到 Mina」「等待對手」。
- [x] 骰子提示支援：「你擲出 4，指向第 4 張」。
- [x] 自己回合且指定位置未翻開時，顯示「翻開第 4 張」。
- [x] 指定位置已處理後，才允許擲骰或進入下一步。
- [x] 非自己回合顯示 disabled 的「等待對手」。
- [x] 底部提示文字依狀態更新，例如「先翻開指定位置的牌」。

驗收：
- [x] 玩家永遠知道目前輪到誰、骰到幾、要處理哪一張牌。
- [x] 不可操作狀態不應出現可點擊主操作。

## P0 - 卡牌詳情彈窗

負責：Modal / Permission Agent

- [x] 依參考圖 9 製作卡牌詳情 modal。
- [x] Modal 背景使用目前遊戲畫面加暗化/模糊，不使用另一張獨立背景。
- [x] 顯示卡牌圖、位置、來源、狀態、效果說明。
- [x] 平常只顯示「關閉」。
- [x] 只有符合以下任一條件時才顯示「使用效果」：
  - 自己的回合，且這張自己的卡牌可以發動效果。
  - 雖非一般操作時段，但規則允許這張自己的卡牌立即發動效果。
- [x] 對手卡牌、非自己卡牌、已使用卡牌、非可用時機，不顯示「使用效果」。

驗收：
- [x] Modal 不是新頁面，關閉後回到原本遊戲畫面與原本狀態。
- [x] 「使用效果」按鈕不會在不能使用的卡牌上出現。

## P0 - 遊戲設定彈窗

負責：Settings Modal Agent

- [x] 依參考圖 10 製作遊戲階段設定 modal。
- [x] Modal 背景使用目前遊戲畫面加暗化/模糊。
- [x] 顯示房號與玩家數。
- [x] 保留操作：「回到 Lobby」「離開房間」「取消」「關閉設定」。
- [x] 移除「複製房號」。
- [x] 移除「邀請朋友」。
- [x] 移除「階段」資訊列。
- [x] 移除「重新連線」。

驗收：
- [x] 遊戲中設定 modal 只保留玩家真正需要的離開/返回/關閉操作。
- [x] 不要和準備畫面的「複製房號」規則混淆：準備畫面保留，遊戲設定移除。

## P1 - 卡牌與圖示資產

負責：Card Art Agent

- [x] 統一卡牌正面樣式：白底紙質、深綠圖示、金色細線。
- [x] 統一卡牌背面樣式：深綠底、金色裝飾、候選序號/位置序號。
- [x] 補齊卡牌類型 icon：加分、扣分、逆轉、防禦、倍數、終局等。
- [x] 卡牌在 grid、列表、底部手牌、modal 內保持同一套比例與語言。

驗收：
- [ ] 同一張牌在不同場景中樣式一致，只改尺寸與資訊密度。

## P1 - 響應式與手機視覺 QA

負責：Verification Agent

- [x] 檢查手機直式 390x844。
- [x] 檢查手機直式 430x932。
- [x] 檢查桌機寬螢幕置中效果。
- [x] 檢查開始、房間、準備、選牌、排牌、2/3/4 人回合、卡牌 modal、設定 modal。
- [x] 確認所有主要文字不爆版、不重疊、不被裝飾物遮住。
- [x] 確認按鈕 disabled / active / loading 狀態清楚。

驗收：
- [x] 提供每個主要畫面的截圖或人工確認記錄。
- [x] 4 人遊戲桌面與兩個 modal 必須列為重點驗收。

## P2 - 動效與操作手感

負責：Interaction Polish Agent

- [x] 選牌時加入翻牌或選中回饋。
- [x] 排牌上下移動時加入輕量過渡。
- [x] 擲骰時加入骰子變化或短動畫。
- [x] 翻牌時加入翻面動效。
- [x] Modal 開關加入淡入淡出，不影響操作速度。

驗收：
- [x] 動畫不能阻礙遊戲流程，也不能讓狀態看起來延遲或錯亂。

## P2 - 文件同步

負責：Documentation Agent

- [x] 更新 `README.md` 的操作流程。
- [x] 補上新流程：開始畫面 -> 房間操作 -> 準備畫面 -> 選牌 -> 排牌 -> 回合 -> 卡牌詳情/設定。
- [x] 補上多人配置說明：2/3/4 人桌面差異。

## 可能還需要補圖的階段

- [ ] 遊戲結束 / 結算畫面參考圖。
- [ ] 使用效果成功後的結果提示參考圖。
- [ ] 錯誤或斷線狀態參考圖。

目前沒有這三類圖片也可以先開工；如果後續要做到完整 polish，再補會更準。

## Bug 修正待辦（2026-05-14）

### 7. 已使用卡片仍需保留卡片效果 icon，並調整已使用狀態背景
負責：Card Used State Visual Agent

- [x] 參考 `Bug/image6.png` 紅圈處，檢查回合階段底部已使用卡片的顯示樣式。
- [x] 已使用卡片可以維持目前簡化資訊，但仍要保留原卡片效果 icon，例如扣 1 分卡片顯示叉叉 icon、加分卡片顯示菱形加號 icon。
- [x] 調整已使用狀態的背景色，不要是目前接近全白的樣式，需更貼合遊戲既有復古紙張、深綠卡牌、柔和陰影的視覺風格。
- [x] 確認已使用標籤、卡片序號、效果 icon 不互相遮擋，並在手機寬度下仍可辨識。
- [x] 驗收標準：使用卡片後，底部對應卡片仍能看出卡片類型 icon，且背景色與整體 UI 風格一致。

### 8. 卡牌詳情視窗出現整個視窗閃爍問題
負責：Modal Flicker Investigation Agent

- [x] 參考 `Bug/image7.png`，檢查卡牌詳情 modal 開啟、關閉、背景遮罩、卡片內容渲染時是否有閃爍。
- [x] 排查是否由動畫 transition / keyframes、overlay backdrop blur、重新 mount/unmount、狀態更新或瀏覽器 repaint/reflow 造成整個視窗短暫閃爍。
- [x] 確認閃爍是否只發生在手機瀏覽器、Vercel 預覽網址、特定卡片詳情，或所有 modal 都會發生。
- [x] 若是動畫問題，需調整 modal 與遮罩動畫，使背景和視窗不會反覆重繪或亮暗跳動。
- [x] 驗收標準：連續開關卡牌詳情 modal 多次時，視窗、遮罩與背景不再出現明顯閃爍。

## 操作順暢度優化待辦（2026-05-15）

### 9. 選牌階段點擊卡牌後等待感過重
負責：Draft Interaction Responsiveness Agent

- [x] 參考使用者回報：選牌階段點一張牌後，卡牌翻開或選中後需要過一陣子才能再點下一張，連續操作不順。
- [x] 建議先閱讀 `Client/app.js` 的 `draftCard(...)`、`pendingAction`、`pollRoomOnce(...)`、`markDraftCardSelected(...)`。
- [x] 建議先閱讀 `Client/game-render.js` 的 `renderDraftPhase(...)`、`renderDraftCard(...)`、draft card disabled 判斷。
- [x] 建議先閱讀 `Client/styles.css` 中 `.draft-card`、`.draft-card:disabled`、`.draft-card.just-selected`、draft phase 相關動畫。
- [x] 檢查是否因為全域 `pendingAction` 讓所有候選卡在單張選牌 API 回來前都被 disabled，造成使用者感覺必須等待。
- [x] 評估是否可以加入更即時的 local feedback / optimistic selected 狀態，讓點擊後立即有反應，同時避免重複送同一張牌。
- [x] 檢查翻牌/選中動畫是否時間過長、是否阻擋 pointer interaction，必要時縮短動畫或解除不必要的操作阻塞。
- [x] 驗收標準：選牌階段連續選 6 張牌時，每次點擊都能立即看到回饋，且不需要明顯等待上一張動畫或同步結束才知道操作生效。

### 10. 遊玩階段按鈕與卡牌操作 pending 體感卡頓
負責：Turn Interaction Responsiveness Agent

- [x] 參考使用者回報：遊玩階段按鈕處理也有卡頓感，包含擲骰、翻牌、使用效果、關閉/開啟卡牌詳情等操作。
- [x] 建議先閱讀 `Client/app.js` 的 `rollTurn(...)`、`handleTargetCardClick(...)`、`useRevealedCard(...)`、`cardUsePending`、`pendingAction`。
- [x] 建議先閱讀 `Client/game-render.js` 的 `renderTurnPhase(...)`、`getRollActionState(...)`、`renderBoardCard(...)`、`renderRevealedCardModal(...)`。
- [x] 建議先閱讀 `Client/styles.css` 中 `.central-roll-button`、`.table-card.target-card`、`.card-modal`、`.card-modal-backdrop`、操作動畫 keyframes。
- [x] 檢查 pending / disabled 狀態是否太廣，是否把不相關操作一起鎖住，導致玩家感覺 UI 沒反應。
- [x] 檢查擲骰、翻牌、使用效果是否需要最短動畫時間或人工等待；若有，確認是否可縮短或改成不阻塞下一步。
- [x] 補強即時回饋：按下後立刻顯示按鈕狀態、目標卡片狀態、loading 文字或局部狀態，不要等下一次 polling 才更新。
- [x] 驗收標準：回合中每個主要操作點擊後 100ms 內有可見回饋，且不相關按鈕/卡牌不會被過度 disabled。

### 11. 前端重繪與 polling 造成的整體操作延遲盤點
負責：Client Render Performance Agent

- [x] 針對整體操作卡頓，盤點是否由頻繁 `replaceChildren(...)`、整個遊戲畫面重 render、polling 回來後重建 DOM、或動畫重繪造成。
- [x] 建議先閱讀 `Client/app.js` 的 `startRoomPolling(...)`、`pollRoomOnce(...)`、`renderCurrentRoom(...)`、`setPending(...)`。
- [x] 建議先閱讀 `Client/game-render.js` 的 `renderGame(...)` 以及各階段 `replaceChildren(...)` 使用位置。
- [x] 建議先閱讀 `Client/styles.css` 中可能造成大量 repaint 的 `backdrop-filter`、大型 `box-shadow`、全畫面動畫、`:has(...)` phase selector。
- [x] 檢查 polling 間隔、force polling、API 操作後是否重複 fetch 或重複 render，避免單次操作造成多次畫面重建。
- [x] 評估是否可保留局部 DOM、減少整棵區塊重建，或至少避免在 pending 狀態反覆重建正在互動的卡片/按鈕。
- [x] 驗收標準：選牌與回合操作時沒有明顯整頁頓一下、按鈕延遲變化、卡片重排跳動；`npm run check` 通過。

## 多語系功能待辦（2026-05-15）

### 12. 新增語言切換功能（中文 / 日語 / 英語）
負責：I18n Language Switch Agent

- [x] 預設語言為中文，使用者可切換成日語或英語。
- [x] 在主頁新增語言切換按鈕或選單，入口需明顯但不干擾「開始」主要操作。
- [x] 語言切換後，主頁、房間操作、準備畫面、選牌、排牌、回合階段、卡牌詳情 modal、設定 modal、錯誤/提示訊息都應逐步納入翻譯範圍。
- [x] 切換後需立即更新目前畫面文字，不需要重新整理頁面。
- [x] 使用者選擇的語言需保存，下次進入遊戲時沿用上次選擇；若沒有保存值則使用中文。
- [x] 建議先閱讀 `Client/index.html`，確認主頁 DOM 與適合放語言切換按鈕的位置。
- [x] 建議先閱讀 `Client/app.js`，確認目前各畫面文字、訊息、localStorage 使用方式與 render 流程。
- [x] 建議先閱讀 `Client/game-render.js`，盤點遊戲階段內所有 hardcoded 中文字串。
- [x] 建議先閱讀 `Client/room-render.js`，盤點房間與玩家清單相關 hardcoded 中文字串。
- [x] 建議先閱讀 `Client/styles.css`，新增語言切換 UI 樣式並確認手機寬度不擠壓主操作。
- [x] 建議建立小型 i18n helper 或字典檔，避免把翻譯散落在各 render 函式內。
- [x] 驗收標準：主頁可切換中文、日語、英語；切換後目前畫面文字立即更新；重新整理後保留使用者選擇；`npm run check` 通過。
